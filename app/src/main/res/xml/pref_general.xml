<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
        android:defaultValue="1"
        android:entries="@array/pref_distance_unit_titles"
        android:entryValues="@array/pref_distance_unit_values"
        android:key="distance_unit_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_distance_unit" />

    <Preference android:title="@string/pref_clear_fav"
        android:key="clear_fav_button"/>

    <Preference android:title="@string/pref_clear_history"
        android:key="clear_history_button"/>

</PreferenceScreen>